<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <!-- Android 12+ (API 31+) 새로운 블루투스 권한 -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:maxSdkVersion="32" />

    <!-- 위치 권한 (Android 11 이하에서 BLE 스캔에 필요) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"
        android:maxSdkVersion="30" />

    <!-- BLE 지원 기능 요구사항 -->
    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />

    <!-- 블루투스 지원 기능 요구사항 -->
    <uses-feature
        android:name="android.hardware.bluetooth"
        android:required="true" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Eggi"
        tools:targetApi="26">

        <!-- ✅ 앱 시작점: SplashActivity -->
        <activity
            android:name=".main.ui.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Eggi">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ✅ 모바일과 통신을 위한 수신 서비스 등록 -->
        <service
            android:name=".common.wear.communication.receiver.WearToMobileReceiverService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.BIND_LISTENER"
                    tools:ignore="WearableBindListener" />
            </intent-filter>
        </service>

        <!-- ✅ 포그라운드 실행 -->
        <service
            android:name=".common.foreground.ForegroundService"
            android:foregroundServiceType="dataSync"
            android:exported="false"
            tools:ignore="ForegroundServicePermission" />

        <!-- ✅ Main 화면 -->
        <activity
            android:name=".main.ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Eggi" />

        <!-- ✅ Landing 화면 -->
        <activity
            android:name=".landing.view.LandingActivity"
            android:exported="true" />

        <!-- ✅ person -->
        <activity
            android:name=".person.ui.PersonActivity"
            android:exported="false" />

        <receiver
            android:name=".common.notification.DailyAlarmReceiver"
            android:exported="false" />
    </application>

</manifest>