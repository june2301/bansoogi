<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- permissions -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />
    <uses-permission android:name="com.samsung.android.permission.health.MONITOR_PPG" />
    <uses-permission android:name="com.samsung.android.service.health.permission.HEALTH_TRACKING" />

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@android:style/Theme.DeviceDefault">

        <uses-library android:name="com.google.android.wearable" android:required="true"/>
        <meta-data android:name="com.google.android.wearable.standalone" android:value="true"/>

        <!-- Calibration/LIVE UI is default launcher -->
        <activity
            android:name="com.example.ppg.presentation.MainActivityCalibAndLive"
            android:exported="true"
            android:theme="@style/MainActivityTheme.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- (optional) original model‑based UI -->
        <activity android:name="com.example.ppg.presentation.MainActivity"
            android:exported="false"
            android:theme="@style/MainActivityTheme.Starting"/>

        <service android:name="com.example.ppg.sensor.RecorderService"
            android:exported="false"
            android:foregroundServiceType="health|dataSync"/>
        <service android:name="com.example.ppg.sensor.CalibRuleService"
            android:exported="false"
            android:foregroundServiceType="health|dataSync"/>
    </application>
</manifest>